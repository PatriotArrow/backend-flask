version: "3.9"

services:
  backend-flask:
    image: patriotarrow/lms-backend:2.0.0  # your Docker Hub username
    ports:
      - "4000:4000"
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    networks:
      - app-network

networks:
  app-network:
    driver: bridge